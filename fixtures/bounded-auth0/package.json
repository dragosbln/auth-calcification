{
  "name": "fixture-bounded-auth0",
  "private": true,
  "version": "0.0.0",
  "description": "Synthetic fixture: a bounded Auth0 (auth0-spa-js) app. Identical AuthPort + app code as bounded-cognito; only the adapter file differs. Demonstrates that the boundary travels across providers.",
  "dependencies": {
    "@auth0/auth0-spa-js": "^2.1.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@testing-library/react": "^14.0.0",
    "vitest": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
