{
  "name": "auth-calcification-harness",
  "private": true,
  "type": "module",
  "description": "External test harness for the auth-calcification-audit skill. Never packaged with the plugin. Zero-dependency scripts run on Node >= 23.6 (native TypeScript type stripping).",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "check": "node check_invariants.ts golden/mixed-edge-cases.audit.json ../fixtures/mixed-edge-cases",
    "mutants": "node mutants.ts"
  }
}
